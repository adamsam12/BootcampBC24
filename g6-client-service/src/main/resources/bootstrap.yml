spring:
  application:
    name: client-service
  cloud:
    config:
      uri: http://config-service:8086 #Direccion donde se ejecuta config-service (Config Server)
      username: nttdata
      password: nttdata
